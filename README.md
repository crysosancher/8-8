# 8x8 KPI APIs

Simple Express template for building KPI APIs.

## Getting Started

```bash
npm install
npm run dev
```

Server runs on `http://localhost:3000` by default.

## Scripts

- `npm run dev`: start with nodemon
- `npm start`: start with Node.js

## Endpoints

- `GET /health`
- `GET /api/kpis`
